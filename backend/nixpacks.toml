[phases.setup]
# tesseract: receipt OCR   libheif: HEIC/HEIF photo support
nixPkgs = ["tesseract", "libheif"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
